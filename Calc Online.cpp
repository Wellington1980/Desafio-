#include <iostream>
#include <cstdlib

// Codigo para calcular em Média de pessoas que acessam ao site por valor investido 

using namespace std;

int main (int argc, char *argv[])
{
	float visualisaram, curtiram , compartilharam, não compartilharam,valor, media;
	
	printf ("Quantos visualisaram? ");
	scanf ("%f", &visualisaram - 12% );
	// Calculo do numero de vizualizações menos 12%, media de quantos curtem 
	
	printf ("Quantos curtiram? ");
	scanf ("%f", &curtiram -  );
	
	printf ("Quantos compartilharam? ");
	scanf ("%f", &curtiram - compartilharam = media );
	
	printf ("Quantos não compartilharam? ");
	scanf ("%f", &não compartilharam - 30% );
	
	soma = (visualisaram, curtiram , compartilharam, não compartilharam);
	
	printf (" O total investido por pessoa foi  R$" soma   );
	
	
	
}
